# Admin Dashboard - لوحة تحكم موقع د. حسين خضير

لوحة تحكم شاملة لإدارة الأخبار والرسائل باستخدام React + Vite

## 🚀 التثبيت والتشغيل

```bash
# تثبيت المكتبات
npm install

# تشغيل الخادم المحلي
npm run dev

# بناء المشروع للإنتاج
npm run build
```

## 📁 هيكل المشروع

```
src/
├── components/         # المكونات
│   ├── Sidebar.jsx
│   └── Layout.jsx
├── pages/              # الصفحات
│   ├── LoginPage.jsx
│   ├── DashboardPage.jsx
│   ├── NewsPage.jsx
│   └── ContactsPage.jsx
├── services/           # خدمات API
│   └── api.js
├── utils/              # أدوات مساعدة
│   └── auth.js
├── App.jsx
└── main.jsx
```

## 🔧 التكوين

انسخ ملف `.env.example` إلى `.env`:

```env
VITE_API_URL=http://localhost:5000/api
```

## 🔑 المستخدم الافتراضي

لإنشاء مستخدم مدير، استخدم MongoDB أو قم بإنشاء script:

```javascript
// يمكنك إضافة مستخدم من خلال MongoDB Compass أو:
{
  "username": "admin",
  "email": "admin@example.com",
  "password": "hashed_password", // استخدم bcrypt
  "role": "admin",
  "active": true
}
```

## ✨ المميزات

- 📊 لوحة معلومات بالإحصائيات
- 📰 إدارة الأخبار (إضافة، تعديل، حذف)
- 📧 متابعة الرسائل وتحديث حالاتها
- 🔒 نظام مصادقة آمن
- 📈 رسوم بيانية للإحصائيات
- 🎨 واجهة مستخدم حديثة وسهلة

## 📦 المكتبات المستخدمة

- React 18
- React Router DOM
- Axios
- Lucide React (الأيقونات)
- Recharts (الرسوم البيانية)
- Tailwind CSS
