# 📘 دليل الاستخدام الكامل - موقع د. حسين خضير

## 📋 نظرة عامة

تم إنشاء مشروع متكامل ومنظم بشكل احترافي يتكون من:

### 🏗️ هيكل المشروع

```
oravil/
├── frontend/              # الواجهة الأمامية للموقع (React + Vite)
│   ├── src/
│   │   ├── components/    # المكونات القابلة لإعادة الاستخدام
│   │   ├── pages/         # صفحات الموقع
│   │   └── services/      # خدمات API
│   ├── package.json
│   └── vite.config.js
│
├── backend/               # API الخلفي (Express + MongoDB)
│   ├── config/            # إعدادات قاعدة البيانات
│   ├── models/            # نماذج البيانات
│   ├── controllers/       # معالجات الطلبات
│   ├── routes/            # مسارات API
│   ├── middleware/        # الوسائط (المصادقة)
│   └── server.js
│
├── admin-dashboard/       # لوحة التحكم (React + Vite)
│   ├── src/
│   │   ├── components/    # مكونات لوحة التحكم
│   │   ├── pages/         # صفحات الإدارة
│   │   ├── services/      # خدمات API
│   │   └── utils/         # أدوات مساعدة
│   ├── package.json
│   └── vite.config.js
│
└── README.md              # هذا الملف
```

---

## 🚀 خطوات البدء السريع

### 1️⃣ المتطلبات الأساسية

قبل البدء، تأكد من تثبيت:
- **Node.js** >= 18.0.0
- **MongoDB** >= 6.0
- **npm** أو **yarn**

### 2️⃣ تثبيت MongoDB

#### Windows:
```powershell
# تحميل MongoDB من الموقع الرسمي
https://www.mongodb.com/try/download/community

# تشغيل MongoDB
mongod
```

#### Linux/Mac:
```bash
# Ubuntu/Debian
sudo apt-get install mongodb

# تشغيل
sudo systemctl start mongodb
```

### 3️⃣ إعداد Backend (API)

```powershell
# الانتقال إلى مجلد Backend
cd backend

# تثبيت المكتبات
npm install

# نسخ ملف البيئة
copy .env.example .env

# تعديل ملف .env (افتحه وغير الإعدادات إذا لزم الأمر)

# تشغيل الخادم
npm run dev
```

الخادم سيعمل على: `http://localhost:5000`

### 4️⃣ إعداد Frontend (الواجهة الأمامية)

افتح نافذة PowerShell جديدة:

```powershell
# الانتقال إلى مجلد Frontend
cd frontend

# تثبيت المكتبات
npm install

# نسخ ملف البيئة
copy .env.example .env

# تشغيل الموقع
npm run dev
```

الموقع سيعمل على: `http://localhost:3000`

### 5️⃣ إعداد Admin Dashboard (لوحة التحكم)

افتح نافذة PowerShell ثالثة:

```powershell
# الانتقال إلى مجلد Admin Dashboard
cd admin-dashboard

# تثبيت المكتبات
npm install

# نسخ ملف البيئة
copy .env.example .env

# تشغيل لوحة التحكم
npm run dev
```

لوحة التحكم ستعمل على: `http://localhost:3001`

---

## 🔑 إنشاء مستخدم المدير الأول

يجب إنشاء مستخدم مدير للوصول إلى لوحة التحكم:

### الطريقة 1: استخدام MongoDB Compass

1. افتح MongoDB Compass
2. اتصل بـ: `mongodb://localhost:27017`
3. أنشئ قاعدة بيانات: `senator-db`
4. أنشئ مجموعة: `users`
5. أضف مستند جديد:

```javascript
{
  "username": "admin",
  "email": "admin@example.com",
  "password": "$2a$10$your_hashed_password_here", // يجب تشفيرها
  "role": "admin",
  "active": true,
  "createdAt": new Date(),
  "updatedAt": new Date()
}
```

### الطريقة 2: استخدام API

سأضيف script لإنشاء مستخدم...

```powershell
# في مجلد backend
node scripts/createAdmin.js
```

---

## 📱 استخدام النظام

### الموقع الأمامي (للزوار)

**الرابط:** `http://localhost:3000`

#### الصفحات المتاحة:

1. **الصفحة الرئيسية** (`/`)
   - عرض معلومات السيناتور
   - الإنجازات والإحصائيات
   - روابط سريعة

2. **السيرة الذاتية** (`/about`)
   - المنصب الحالي
   - المؤهلات العلمية
   - الإنجازات والمبادرات

3. **الأخبار** (`/news`)
   - عرض جميع الأخبار والفعاليات
   - التفاصيل الكاملة لكل خبر

4. **التواصل** (`/contact`)
   - نموذج إرسال الشكاوى والطلبات
   - معلومات التواصل
   - ساعات العمل

---

### لوحة التحكم (للإدارة)

**الرابط:** `http://localhost:3001`

#### تسجيل الدخول:
- اسم المستخدم: `admin`
- كلمة المرور: (التي قمت بإنشائها)

#### الصفحات:

1. **الرئيسية** (`/`)
   - إحصائيات شاملة
   - رسوم بيانية
   - أحدث الرسائل

2. **إدارة الأخبار** (`/news`)
   - إضافة خبر جديد
   - تعديل الأخبار الموجودة
   - حذف الأخبار
   - نشر/إلغاء نشر

3. **إدارة الرسائل** (`/contacts`)
   - عرض جميع الرسائل
   - تصنيف حسب الحالة
   - تحديث حالة الرسائل
   - إضافة ملاحظات

---

## 🔌 API Endpoints

### الأخبار (News)

```http
GET    /api/news           # الحصول على جميع الأخبار
GET    /api/news/:id       # الحصول على خبر محدد
POST   /api/news           # إنشاء خبر جديد (مدير فقط)
PUT    /api/news/:id       # تحديث خبر (مدير فقط)
DELETE /api/news/:id       # حذف خبر (مدير فقط)
```

### الرسائل (Contact)

```http
POST   /api/contact                # إرسال رسالة جديدة
GET    /api/contact                # الحصول على جميع الرسائل (محمي)
GET    /api/contact/:id            # الحصول على رسالة محددة (محمي)
PATCH  /api/contact/:id/status     # تحديث حالة رسالة (محمي)
DELETE /api/contact/:id            # حذف رسالة (محمي)
```

### المصادقة (Auth)

```http
POST   /api/auth/login      # تسجيل الدخول
GET    /api/auth/me         # الحصول على المستخدم الحالي (محمي)
POST   /api/auth/users      # إنشاء مستخدم جديد (مدير فقط)
```

### الإحصائيات (Stats)

```http
GET    /api/stats           # الحصول على الإحصائيات (محمي)
```

---

## 🛠️ أوامر مفيدة

### Frontend

```powershell
npm run dev      # تشغيل وضع التطوير
npm run build    # بناء للإنتاج
npm run preview  # معاينة البناء
```

### Backend

```powershell
npm run dev      # تشغيل مع nodemon (تحديث تلقائي)
npm start        # تشغيل عادي
```

### Admin Dashboard

```powershell
npm run dev      # تشغيل وضع التطوير
npm run build    # بناء للإنتاج
npm run preview  # معاينة البناء
```

---

## 🎨 التخصيص

### تغيير الألوان

في ملفات Tailwind CSS، يمكنك تعديل:

```javascript
// tailwind.config.js
theme: {
  extend: {
    colors: {
      // أضف ألوانك المخصصة هنا
    }
  }
}
```

### إضافة صفحات جديدة

1. أنشئ ملف في `src/pages/`
2. أضف المسار في `App.jsx`
3. أضف رابط في `Header.jsx`

---

## 🔒 الأمان

- كلمات المرور مشفرة باستخدام bcrypt
- المصادقة عبر JWT
- CORS محدد للنطاقات المسموحة
- Rate limiting لمنع الهجمات
- Helmet للحماية من الثغرات الشائعة

---

## 📦 النشر (Deployment)

### Frontend & Admin

يمكن نشرهما على:
- **Vercel** (مجاني)
- **Netlify** (مجاني)
- **GitHub Pages**

```powershell
npm run build
# ارفع مجلد dist
```

### Backend

يمكن نشره على:
- **Heroku**
- **Railway**
- **DigitalOcean**
- **AWS**

---

## 🐛 حل المشاكل الشائعة

### المشكلة: MongoDB لا يعمل
```powershell
# تأكد من تشغيل MongoDB
mongod --version
# إذا لم يعمل، أعد تثبيته
```

### المشكلة: خطأ CORS
```javascript
// تأكد من إضافة الرابط في backend/.env
FRONTEND_URL=http://localhost:3000
ADMIN_URL=http://localhost:3001
```

### المشكلة: لا يمكن تسجيل الدخول
```javascript
// تأكد من إنشاء مستخدم في MongoDB
// تأكد من JWT_SECRET في .env
```

---

## 📞 الدعم

للمساعدة أو الاستفسارات:
- راجع الملفات README.md في كل مجلد
- تحقق من الـ console للأخطاء
- تأكد من تشغيل جميع الخدمات (MongoDB, Backend, Frontend)

---

## ✅ Checklist للتشغيل

- [ ] MongoDB مثبت ويعمل
- [ ] Node.js مثبت (>= 18)
- [ ] تم تثبيت dependencies في Backend
- [ ] تم تثبيت dependencies في Frontend
- [ ] تم تثبيت dependencies في Admin Dashboard
- [ ] تم نسخ ملفات .env
- [ ] تم إنشاء مستخدم مدير
- [ ] Backend يعمل على port 5000
- [ ] Frontend يعمل على port 3000
- [ ] Admin Dashboard يعمل على port 3001

---

## 🎉 تهانينا!

مشروعك جاهز الآن! يمكنك البدء في استخدامه وتخصيصه حسب احتياجاتك.

**ملاحظة:** هذا المشروع جاهز للتطوير. للنشر في الإنتاج، تأكد من:
- تغيير JWT_SECRET
- استخدام قاعدة بيانات MongoDB سحابية (MongoDB Atlas)
- تفعيل HTTPS
- تحسين الأداء
