# Security Policy

## Supported Versions

نحن نوفر تحديثات الأمان للإصدارات التالية:

| Version | Supported          |
| ------- | ------------------ |
| 1.0.x   | :white_check_mark: |

## Reporting a Vulnerability

إذا اكتشفت ثغرة أمنية، يرجى عدم فتح Issue عام. بدلاً من ذلك:

### كيفية الإبلاغ

1. **لا تفتح Issue عام** - الثغرات الأمنية يجب أن تبقى سرية حتى يتم إصلاحها
2. **أرسل بريد إلكتروني** إلى: security@example.com
3. **قدم معلومات مفصلة**:
   - وصف الثغرة
   - خطوات إعادة الإنتاج
   - التأثير المحتمل
   - اقتراحات للإصلاح (اختياري)

### ما يمكن توقعه

- **الرد الأولي**: خلال 48 ساعة
- **التقييم**: خلال 5 أيام عمل
- **الإصلاح**: حسب خطورة الثغرة (عادة خلال 30 يوم)
- **الإشعار**: سيتم إعلامك عند إصدار الإصلاح

### أنواع الثغرات المهمة

نحن مهتمون بشكل خاص بالإبلاغ عن:

- SQL Injection
- XSS (Cross-Site Scripting)
- CSRF (Cross-Site Request Forgery)
- Authentication/Authorization bypass
- Sensitive data exposure
- Server-Side Request Forgery (SSRF)
- Remote Code Execution (RCE)

### خارج النطاق

الأمور التالية لن تُعتبر ثغرات أمنية:

- Denial of Service attacks
- Social engineering
- Physical attacks
- Spam

## Best Practices للمطورين

### Backend
- استخدم HTTPS فقط في الإنتاج
- لا تحفظ passwords بصيغة plain text
- استخدم JWT tokens مع expiration time مناسب
- قم بـ validate جميع المدخلات
- استخدم parameterized queries لتجنب SQL injection

### Frontend
- لا تحفظ tokens في localStorage إذا كان حساس جداً
- استخدم Content Security Policy
- تحقق من صحة البيانات قبل إرسالها
- لا تعرض error messages مفصلة للمستخدمين

### Environment Variables
- **لا تضع أبداً** `.env` في Git
- استخدم `.env.example` كنموذج فقط
- غيّر جميع الـ secrets في الإنتاج
- استخدم strong JWT_SECRET (32+ characters random)

### Dependencies
- حدّث المكتبات بانتظام
- استخدم `npm audit` للتحقق من الثغرات
- لا تستخدم مكتبات قديمة أو غير محدثة

## Security Checklist قبل Deploy

- [ ] تم تغيير جميع الـ default passwords
- [ ] تم تحديث JWT_SECRET
- [ ] MONGODB_URI يستخدم credentials آمنة
- [ ] CORS محدد بدومينات صحيحة
- [ ] HTTPS مفعّل
- [ ] Rate limiting مفعّل
- [ ] Helmet middleware مفعّل
- [ ] لا توجد ملفات .env في Git
- [ ] تم مراجعة جميع الـ permissions
- [ ] تم اختبار الأمان

## شكراً

نشكرك على المساعدة في الحفاظ على أمان المشروع! 🙏
